services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vik
      POSTGRES_PASSWORD: vik
      POSTGRES_DB: vik
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
